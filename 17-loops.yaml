- name: Install packages
  hosts: local
  connection: local
  tasks:
  - name: Installing packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - nginx
    - mysql
    - nodejs